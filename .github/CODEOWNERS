#
# Code owners are automatically requested to review pull requests that touch
# the files they own. The last matching pattern wins, so order the rules from
# the most general to the most specific.
#
# Every account or team named here MUST have write access to the repository,
# else GitHub silently ignores the rule.
#
# https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#

# Default owner for everything in the repository.
*                       @kieranpotts

# `settings.yml` is applied to the repository's actual GitHub configuration
# by the `apply-settings` workflow, using an admin-scoped token. In effect,
# this escalates anyone who can push the `settings.yml` file to the default
# branch to the admin role, without giving that user explicit admin privileges.
# Therefore it is RECOMMENDED to explicitly define the codeowners for this
# specific file. These users will need to approve all changes to the file
# introduced to the default branch.
/.github/settings.yml   @kieranpotts

# Examples of narrower ownership. Delete or replace these.

# Build and delivery pipelines.
#/.github/workflows/    @kieranpotts

# Infrastructure as code.
#/infra/                @kieranpotts

# Documentation.
#/docs/                 @kieranpotts
